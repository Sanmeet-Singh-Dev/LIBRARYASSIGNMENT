<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Library"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".FeedbackActivity"
            android:label="Feedback"></activity>
        <activity android:name=".AppActivity"
            android:label="App Section"/>
        <activity android:name=".BooksActivity"
            android:label="Book Section"/>
        <activity android:name=".EntertainmentActivity"
            android:label="Entertainment Section"/>
        <activity android:name=".LoginActivity"
            android:label="Login/Register"/>
        <activity
            android:name=".Homepage"
            android:label="Homepage" />
        <activity
            android:name=".Register"
            android:label="Registration" />
        <activity
            android:name=".IntroductionAnim"
            android:label="Library App">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>